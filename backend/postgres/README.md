# Important: Read this.

Note that this folder is simply some sql code to setup the database, as I used Supabase's SQL editor and manually 
copypasted code from this folder into that editor. This code is not meant to be run by itself.